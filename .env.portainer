# CLIENT_NAME - Utilisé pour préfixer tous les conteneurs et volumes
CLIENT_NAME=client

# ======================================
# CONFIGURATION BASE DE DONNÉES
# ======================================
POSTGRES_DB=erp_btp
POSTGRES_USER=erp_user
POSTGRES_PASSWORD=ChangeMe123!

# ======================================
# CONFIGURATION BASE DE DONNÉES ABONNEMENTS
# ======================================
# Base de données externe pour la gestion des abonnements
SUBSCRIPTION_DB_HOST=176.131.66.167
SUBSCRIPTION_DB_PORT=5433
SUBSCRIPTION_DB_NAME=erpbtp_clients
SUBSCRIPTION_DB_USER=fred
SUBSCRIPTION_DB_PASSWORD=Jbvf2023@

# ======================================
# CONFIGURATION APPLICATION
# ======================================
# Port exposé pour l'application (changez pour chaque client)
APP_PORT=8081

# URL publique de l'application
APP_URL=http://localhost:8081

# Clé secrète pour les sessions (générez avec: openssl rand -base64 32)
SECRET_KEY=changez-cette-cle-secrete-32-caracteres

# ======================================
# CONFIGURATION EMAIL (OPTIONNEL)
# ======================================
# Serveur SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Identifiants SMTP
SMTP_USERNAME=votre-email@gmail.com
SMTP_PASSWORD=votre-mot-de-passe-app

# Email expéditeur
SMTP_FROM=noreply@erp-btp.com

# ======================================
# CONFIGURATION GITHUB (si privé)
# ======================================
GITHUB_OWNER=fvictoire59va
